spring:
  flyway:
    enabled: false
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2://mem:db;DB_CLOSE_DELAY=-2
    username: sa
    password: sa
  jpa:
    hibernate:
      ddl-auto: create-drop
